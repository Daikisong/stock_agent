# 외부 검토 번들 안내

## 가장 빠른 검토 순서

1. `문제점과수정결과.md`
2. `000660_score_receipt.json`
3. `000660_stagecourt_receipt.json`
4. `000660_component_memos_compact.jsonl`
5. `000660_judge_decisions_compact.jsonl`
6. `000660_red_team_receipt.json`
7. `000660_synthesis_receipt.json`
8. `000660_supervisor_receipt.json`
9. `000660_saturation_reviews_compact.jsonl`
10. `identical_rerun_audit.json`

## raw와 과거 재현 자료

기준 원본 390MB와 2,729개 raw snapshot 파일은 이미 Git에 추적돼 있다.

```text
output/researcher_mode/c06/2026-07-12-clean-v8/
```

Gate 1 동안 source corpus, fetch ledger, 36 documents, 2,190 facts와 claim-fact links는 원본과 byte-identical하다. 외부 검토 요청에 맞춰 copy-on-write output과 별도 worktree에서 빠졌던 과거 테스트 입력·page cache도 강제 추적한다. 신규 결과는 이 폴더에 loss-accounted compact 형태로도 제공한다.

- 7개 component memo 원문
- 21개 Judge 결정 원문
- 7개 final component 결정
- Business model / Red Team / synthesis / Supervisor
- saturation A/B/Independent와 certificate
- Stage primitive mapping
- deterministic score / Stage receipt
- 동일 입력 무변동 증명
- 수정 코드·테스트와 문제 진단

GitHub 단일 blob 제한보다 큰 `claim_provenance.jsonl` 하나는 내용 전체를 deterministic gzip으로 추적한다. 다음 명령으로 원본 SHA-256과 byte-identical하게 복원된다.

```bash
bash scripts/restore_external_review_artifacts.sh
```

전체 root별 파일 수·크기·roster SHA-256은 `ignored_artifact_reproduction_manifest.json`에 있다. 즉 ignore 때문에 검증 자료가 조용히 빠지는 상태로 게시하지 않는다.

## 해시 검증

`external_review_bundle_manifest.json`에는 번들의 각 파일 크기와 SHA-256이 있다. 기준 원본과 copy-on-write source/fact 불변성은 `current_fact_roster_receipt.json`, `current_request_response_audit.json`, `identical_rerun_audit.json`에 있다. 전체 테스트 결과는 `full_test_receipt.json`에 있다.

## 주의

collaboration envelope의 `agent_surface=CODEX_COLLABORATION_SUBAGENT`는 bridge schema의 고정 transport label이다. 이번 마무리에는 새 subagent를 spawn하지 않았고, root가 schema 검증을 통과하는 응답을 직접 작성했다. 해당 사실은 `current_request_response_audit.json`과 final review에 기록한다.
