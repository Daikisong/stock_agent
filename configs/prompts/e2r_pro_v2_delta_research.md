이전 full-thesis dossier 전체를 다시 쓰지 마라. 동일 target의 이전 as_of 이후 새 fact, counter, resolution, supersession과 영향받는 mandatory question family만 조사한다. 변화가 없는 question은 제공된 hash-verified closure를 재사용한다.

stale primitive, 새 trigger/event, monitoring/future-event gap을 우선하고, 동일 snapshot이면 새 검색이나 fetch를 만들지 않는다. 기존 accepted fact를 삭제하지 않고 delta lineage만 append한다. score와 Stage는 계산하거나 제안하지 않는다.

{{COMPILED_CONTEXT}}
